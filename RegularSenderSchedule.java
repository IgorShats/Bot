import java.io.IOException;
import java.util.Calendar;
import java.util.Date;
import java.util.TimeZone;

public class RegularSenderSchedule {
    int i = 0;
    public void SendToTeacher(TeacherRegister register) throws IOException, InterruptedException {

/*        ExelParsTeacher exelParsTeacher = new ExelParsTeacher();
        Date date = new Date();
        date.
        Calendar calendar = Calendar.getInstance();
        calendar.setTime(date);
        calendar.setTimeZone(TimeZone.getTimeZone("MSK"));

        i = Calendar.DAY_OF_WEEK;

        if (Calendar.DAY_OF_WEEK == 7) {
            i = 1;
        }
        System.out.println(calendar.HOUR_OF_DAY);
        if (Calendar.HOUR_OF_DAY == 4) {
            for (TeacherUser user : register.longBooleanHashMap.values())
                exelParsTeacher.readWorkbook(user.getName());

        }
        Thread.sleep(86399900);
        SendToTeacher(register);*/

    }
}
